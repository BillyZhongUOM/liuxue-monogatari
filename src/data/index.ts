// Content registry. The engine imports game content only through this barrel so
// new data files can grow without touching engine code.
export { ACTIONS, ACTION_BY_ID } from './actions';
export { EVENTS, EVENT_BY_ID } from './events';
export { ENDINGS, ENDING_BY_ID } from './endings';
export { TRAITS, TRAIT_BY_ID } from './traits';
